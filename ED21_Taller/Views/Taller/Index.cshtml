@{
    ViewData["Title"] = "Inicio";
}
<partial name="_ModalAuto.cshtml" />
<partial name="_ModalMoto.cshtml" />
<div class="d-flex justify-content-center">
    <h1 class="display-4"><span class="badge badge-info">¿Qué desea hacer?</span></h1>
</div>
<div class="d-flex justify-content-center mt-2">
    <p class="lead">
        Bienvenido/a al taller. Puede crear un nuevo presupuesto o ver los presupuestos creados, junto a sus detalles.
    </p>
</div>
<div class="d-flex justify-content-center">
    <button type="button" class="btn btn-primary btn-lg m-4" id="show">
        Crear nuevo presupuesto
    </button>
    <a href="/Taller/Presupuestos" class="btn btn-warning btn-lg m-4">Lista de presupuestos</a>
</div>

<div style="display:none;" class="container" id="elegir">
    <div class="d-flex justify-content-center mt-4">
        <h2><span class="badge badge-info">Para generar un nuevo presupuesto, tiene dos opciones:</span></h2>
</div>
    <div class="d-flex justify-content-center">
        <div class="d-flex flex-row flex-wrap mt-4">
            <div class="card d-flex flex-column mb-3 mr-3">
                <h4 class="card-header">Opción 1: Seleccionar un vehículo existente</h4>
                <div class="card-body col mt-3">
                    <form asp-controller="Taller" asp-action="Presupuestar" method="get">
                        <select name="vehiculo" class="js-example-basic-single custom-select" id="vehiculo">
                            <option value="" selected disabled>Seleccione un vehículo</option>
                            @foreach (var vehiculo in ViewBag.Vehiculos)
                            {
                                <option value="@vehiculo.Id">@vehiculo.Patente - @vehiculo.Marca @vehiculo.Modelo</option>
                            }
                        </select>
                        <button type="submit" class="btn btn-success btn-block btn-lg mt-4">Iniciar presupuesto</button>
                    </form>
                </div>
            </div>
            <div class="card d-flex flex-column mr-3">
                <h4 class="card-header">Opción 2: Agregar un nuevo vehículo</h4>
                <div class="card-body d-flex flex-wrap flex-column">
                    <button type="button" class="btn btn-danger btn-lg ml-2 mr-2 mb-4" data-toggle="modal" data-target="#modalAuto">
                        <i class="bi-plus-circle"></i> Nuevo automóvil
                    </button>
                    <button type="button" class="btn btn-danger btn-lg ml-2 mr-2" data-toggle="modal" data-target="#modalMoto">
                        <i class="bi-card-list"></i> Nueva motocicleta
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>